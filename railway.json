{
  "build": {
    "builder": "custom",
    "buildCommand": "docker-compose build"
  },
  "deploy": {
    "preDeployCommand": ["docker-compose up"],
    "startCommand": "",
    "healthcheckPath": "/",
    "healthcheckTimeout": 100,
    "restartPolicyType": "always"
  }
}